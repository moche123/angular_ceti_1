import { OrdenarEdadPipe } from './ordenar-edad.pipe';

describe('OrdenarEdadPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarEdadPipe();
    expect(pipe).toBeTruthy();
  });
});
